# Используем официальный образ Node.js
FROM node:16

# Устанавливаем рабочую директорию
WORKDIR /app

# Копируем package.json и package-lock.json
COPY frontend/package*.json ./

# Устанавливаем зависимости
RUN npm install

# Копируем весь исходный код в контейнер
COPY frontend/ .

# Строим приложение (можно пропустить, если хотите запускать в режиме разработки)
# RUN npm run build

# Открываем порт 5137, который используется Vite по умолчанию
EXPOSE 5137

# Команда для запуска приложения в режиме разработки (Vite будет работать на порту 5137)
CMD ["npm", "run", "dev"]
